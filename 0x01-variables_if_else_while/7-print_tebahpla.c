#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Reverse numbers'
 * Return: Always 0 (Success)
 */
int main(void)
{	char cha;

	for (cha = 'z'; cha >= 'a'; cha--)
	{
	printf("%c", cha);
	}
	printf("\n");
	return (0);
}
