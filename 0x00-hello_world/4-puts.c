#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Code prints strings using the puts function'
 * Return: Always 0 (Success)
 */
int main(void)
	{char str[] = "\"Programming is like building a multilingual puzzle";
		puts(str);
		return (0); }
